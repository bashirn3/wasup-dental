"use client";

/**
 * Client-side fallback if the beforeInteractive bypass script did not run.
 * The heavy lifting is in ClerkDevBypassScript + SsoCallbackClient.
 */
export default function ClerkDevBypass() {
  return null;
}
